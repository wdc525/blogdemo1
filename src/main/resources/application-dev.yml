spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 888

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

logging:
  level:
    root: info
    com.inet: debug
  file:
    name: log/blog-dev.log